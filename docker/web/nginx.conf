server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Прокси на CRUD api, чтобы страница ходила на тот же origin без CORS
    location /api/ {
        proxy_pass http://api:8000/;
        proxy_set_header Host $host;
    }
}
