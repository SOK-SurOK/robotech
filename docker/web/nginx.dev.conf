server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Dev-режим: api запущен локально на хосте, а не в сети compose
    location /api/ {
        proxy_pass http://host.docker.internal:8000/;
        proxy_set_header Host $host;
    }
}
